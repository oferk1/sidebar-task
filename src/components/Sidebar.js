import React, { PropTypes } from 'react'
import { Link } from 'react-router'

const User = ({ user }) => {

  return (
    <div className="User">
      SIDEBAR***
    </div>
  )
}

export default User
